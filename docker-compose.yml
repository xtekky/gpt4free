version: '3'

services:
  gpt4free:
    image: gpt4free:latest
    shm_size: 2gb
    build:
      context: .
      dockerfile: docker/Dockerfile
      cache_from:
        - gpt4free:latest
    volumes:
      - .:/app
    ports:
      - '8080:80'
      - '1337:1337'
      - '7900:7900'